# Vite carga .env.local en todos los modos (incluido production), así que este
# archivo tiene que existir para sobreescribir el VITE_API_URL de desarrollo
# (ver frontend/.env.local) cuando se compila para producción.
VITE_API_URL=https://delivery.prosello.com.mx/api/v1

# Conexión al servidor Reverb del backend (spec inDriver tenant/018), mismas credenciales que
# panda_express (panda_expess/.env.production). Reverb todavía no corre en el VPS
# (deploy/README.md, pendiente) — mientras tanto el Panel no conecta y sigue funcionando sin
# tiempo real.
VITE_REVERB_APP_KEY=w4herk2g3hwdipplffhj
VITE_REVERB_HOST=delivery.prosello.com.mx
VITE_REVERB_PORT=443
VITE_REVERB_SCHEME=https
