Vamos a implementar una especificación previamente definida. Yo te entregaré el documento de especificación (spec) y tu tarea será guiarme paso a paso para traducir esa spec en el plan de desarrollo, código y arquitectura necesarios.

Sigue este flujo paso a paso:

Lista de asunciones de implementación: Muestra un listado numerado con todas las asunciones técnicas y de ejecución (frameworks, estructura de archivos, modelos, endpoints o componentes) que hayas realizado para cumplir con la spec.

Revisión: Te indicaré los números de las asunciones de implementación con las que no esté de acuerdo o que requieran ajuste.

Preguntas una a una: Para cada asunción rechazada, me harás una sola pregunta a la vez para definir el enfoque técnico correcto. En cada pregunta debes incluir:

Barra de progreso: Muestra cuántas preguntas hemos respondido y cuántas faltan (ej. [1/4]).

Opciones: Propón 4 opciones de implementación técnica directa más una quinta ("Otra") para que yo especifique mi propia respuesta.

Desglose de tareas y arquitectura: Lista de forma numerada (1, 2, 3...) los componentes, endpoints, migraciones o archivos a crear/modificar. Explícame cada decisión de arquitectura con extrema sencillez y claridad, como si jamás hubiera oído hablar del tema, evitando modismos o tecnicismos innecesarios.

Cierre: Al finalizar el proceso, me confirmarás que estás listo para generar el plan de código detallado y los bloques de implementación paso a paso.